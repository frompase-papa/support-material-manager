// 設定のひな型。
//
// このファイルをコピーして同じフォルダに `config.js` を作り、値を埋めてください。
// `config.js` は .gitignore 済みなので GitHub には上がりません。
// （タブレット用の zip には config.js を必ず含めること）

self.SMM_CONFIG = {
  // 本番URL
  API_BASE: "https://support-material-manager.vercel.app",
  // Vercel の環境変数 STUDY_API_KEY と同じ値
  API_KEY: "ここにAPIキーを入れる",
};
